# TianjiTown 反馈修复 TODO

来源：`report.md` 与 `testserver` 服务器数据交叉分析。

优先级说明：P0 为阻断功能或经济漏洞，P1 为主要体验问题，P2 为显示与配置问题。

## 数据结论

- SQLite `quick_check=ok`，无外键违规；当前问题不是数据库损坏。
- “塔迪斯”申请处于 `PROVISION_FAILED`，已记录 7 次相同创建失败。
- 创建失败并非旧小镇领地未释放，而是申请代码 `tds` 与现有玩家 Residence `tds` 重名；现有领地主人为 `Infinity_XueLing`，边界与新申请不同。
- `kml` 已向成员和访客投影 `use`、`container` 权限；服务台不能使用更可能是交互事件被 Residence 取消后 TianjiTown 提前返回。
- QuickShop 3 笔税款合计 5,425 元，并获得等额 5,425 元服务器补贴；1:1 配捐导致“半价捐款”风险成立。

## P0：阻断与经济问题

### 4. 修复成员和访客无法使用小镇服务台

- [x] 将已注册服务台的识别放到 `event.isCancelled()` 判断之前。
- [x] 只允许“配置坐标存在且 PDC 服务台 ID 完全一致”的讲台绕过 Residence 的取消状态。
- [x] TianjiTown 自行取消原版讲台操作并打开小镇菜单。
- [x] 其他讲台和方块继续遵守 Residence 权限。

### 8. 为小镇服务台增加独立爆炸保护

- [x] 监听 TNT、苦力怕等实体爆炸以及方块爆炸事件。
- [x] 仅从爆炸破坏列表中移除已注册服务台方块，不取消整场爆炸。
- [x] 阻止活塞移动服务台。
- [x] 保留 Residence 的 `explode:false`、`tnt:false`、`creeper:false` 作为领地级保护，但不依赖它保护领地外服务台。

### 10. 领地名重复时提示申请人
- [x] 领地可行性检测提前至申请人选择小镇初始领地单元时。
- [x] 领地名重复时提示申请人：“领地名与已存在的领地重复。”

### 16. 修复批准申请后停留在“等待服务器响应”

- [x] 点击批准后立即切换到只读的“正在创建小镇”页面。
- [x] 将创建流程统一返回 `ProvisionResult`，所有成功、业务失败、提前返回和异常路径都必须完成 UI 回调。
- [x] 成功后显示成功页，失败后显示具体原因及可执行的恢复操作。
- [x] 添加超时页面和“返回审核列表”按钮。
- [x] 重试继续使用幂等键，防止重复扣款或重复创建。
- [x] 记录审批开始、领地投影、数据库完成和 UI 回调时间点。

### 17. 为 `PROVISION_FAILED` 申请增加恢复与强制解锁

- [x] 增加管理员“解除锁定并要求修改”。
- [x] 增加管理员“取消申请并退款”。
- [x] 增加管理员“强制清理失败申请”。
- [x] 解锁前验证临时小镇仍为 `PROVISIONING`，且不存在正确创建的受控 Residence 投影。
- [x] 安全回滚临时小镇、成员和领地单元，禁止只改状态或手工删除数据库行。
- [x] 将已收申请费转入明确的托管状态；再次批准时复用，取消时退款。
- [x] 解锁后将申请转为 `NEED_CHANGES`，允许修改名称、代码、选址和成员。
- [x] 再次批准前重新执行名称、世界、边界和碰撞预检。
- [x] 所有解锁、退款和强制清理操作写入审计日志。

### 22. 限制 QuickShop 税收等额补贴套利

- [x] 为税收补贴增加双重限额：每周限额 50000、每 12 小时限额 5000。
- [x] 将每周补贴限额和每 12 小时补贴限额加入配置文件，支持独立配置。
- [x] 在公共资产页面的公共资产说明中显示当前剩余补贴限额和限额刷新时间。
- [x] 补贴发放时校验对应周期的剩余额度，并在周期刷新后恢复可用额度。

## P1：主要功能与体验

### 3. 所有领地按钮改为替玩家执行 `res tp`

- [x] 梳理不同界面中的多个领地相关按钮，确保所有此类按钮都纳入改造。
- [x] 从数据库取得当前小镇的受控 Residence 名称。
- [x] 所有领地传送按钮均替玩家执行 `res tp <领地名>`。
- [x] 每个领地按钮在 Residence 传送成功后再显示领地边界预览。
- [x] 与第 18、21 条一起完成，避免命令保护拦截系统触发的传送。

### 5. ESC 只退出菜单，不执行返回

- [x] “返回”保留为普通导航按钮。
- [x] `exitAction` 使用独立的关闭动作，不再复用返回按钮。
- [x] ESC 只销毁菜单会话并关闭界面，不执行返回、取消编辑或其他业务动作。
- [ ] 添加“点击返回”和“按 ESC”行为分离测试。

### 6. 补齐投票通知和配置文案

- [x] 创建投票后，根据冻结选民名单通知所有在线有效选民。
- [x] 通知包含发起者、投票类型、目标、截止时间、通过票数及“前往投票”按钮。
- [x] 离线玩家登录后继续收到待处理投票提醒。
- [x] 将“赞成”“反对”“终止投票”、状态、票数、目标等文字和提示全部迁入 `messages.yml`。
- [x] 启动时校验必需的投票配置键。

### 7. 支持批量选择领地扩张单元

- [x] 调整扩张界面交互：点击一个或多个代表领地单元格的按钮进行选择，再点击“确认”按钮提交扩张。
- [x] 每次按钮按下后刷新按钮上的物品标记，准确反映对应单元格的选中状态。
- [x] 实时显示已选数量、总价格、剩余容量和地图预览。
- [x] 提交时重新校验容量、碰撞及选区与现有领地的连通性。
- [x] 数据库一次性扣除总价并创建全部扩张记录。
- [x] Residence 投影失败时整体回滚新增区域和资金。
- [x] 扩张完成后仍停留在领地扩张界面，并刷新单元格状态及相关信息。
- [x] 不得通过连续调用多次单格扩张模拟批量操作，避免部分成功和重复扣款。

### 11. 邀请错误显示玩家名而不是 UUID

- [x] 存储层返回结构化冲突信息：玩家 UUID、冲突类型、所属小镇。
- [x] UI 通过在线玩家、离线玩家缓存或 `usercache.json` 解析玩家名。
- [x] 解析失败时显示“未知玩家（UUID 后 8 位）”。
- [x] 保存前进行友好预检，事务提交时仍保留最终冲突校验。

### 12. 取消成员邀请选择时保留草稿

- [x] 取消玩家选择只返回成员步骤，不删除表单会话。
- [x] 区分“返回”“保存草稿并退出”“放弃草稿”。
- [x] 切换步骤或退出前读取并保存当前输入。

### 13. 申请填写每一步都可保存草稿

- [x] 新增独立的 `application_form_drafts` 持久化表，允许字段不完整。
- [x] 保存当前填写步骤和更新时间。
- [x] 第 1、2、3 步均提供“保存草稿并退出”。
- [x] 点击上一步或下一步时自动保存当前步骤。
- [x] 重新进入申请时恢复最后步骤和所有已填内容。
- [x] 只有资料完整且验证通过后才创建正式申请并发送成员邀请。

### 15. 审核详情补充申请人信息

- [x] 显示申请人玩家名和 UUID。
- [x] 显示申请编号、提交时间、更新时间和当前状态。
- [x] 初始成员显示玩家名及“待确认/已确认/已拒绝”状态。
- [x] 玩家名无法解析时才以 UUID 作为辅助信息。

### 18. 增加小镇传送点设置功能

- [x] 镇长界面增加“设置领地传送点”。
- [x] 玩家必须站在本镇有效 Residence 内。
- [x] 校验落脚点、头部空间及危险方块。
- [x] 通过 Residence API 设置 `TPLoc`，不向普通玩家开放修改命令。
- [x] 创建小镇时自动设置安全的默认传送点。
- [x] 记录操作者、世界和坐标审计。

### 19. 修复账本操作人显示 UUID

- [x] 建镇申请费入账时传递申请人的玩家名，不再使用 `applicantId().toString()`。
- [x] UI 检测 `actor_name` 是否为 UUID；若是则通过 `actor_uuid` 动态解析显示名。
- [x] 使用可靠的玩家缓存一次性回填可确认的历史记录。
- [x] 无法确认的历史记录保留原值，不进行猜测性覆盖。

### 20. 修复 QuickShop 税收操作人显示 UUID

- [x] QuickShop 税收事件 DTO 增加 `receiverName`。
- [x] 写入账本时同时保存玩家名和 UUID。
- [x] 历史记录保留交易发生时的玩家名，可选显示当前玩家名。
- [x] 回填当前记录：`CUMULUS_BALL` 两笔、`Yagemi_Hso` 一笔。
- [x] 保持 Jobs 和 GlobalMarketPlus 已有的正确玩家名写入逻辑。

### 21. 允许玩家手动执行 `res tp` 前往任意有效小镇

- [x] 精确解析 `/res` 和 `/residence` 子命令，不再按整条命令粗略阻止。
- [x] 明确允许 `/res tp <有效小镇Residence名>`。
- [x] 仅允许传送到数据库中状态为 `ACTIVE` 的受控小镇。
- [x] 继续阻止 `remove`、`set`、`pset`、`area`、`select`、`tpset` 等修改命令。
- [x] 安全点、冷却和费用继续交由 Residence 处理。

## P2：显示与配置

### 1. 修复公共 Buff 滑动条数值格式

- [x] 将 `duration-format` 从 `&f%.0f 周` 改为 `&f%s 周`。
- [x] 将 `intensity-format` 从 `&f等级 %.0f` 改为 `&f等级 %s`。
- [x] 保持滑动步长为 1。
- [x] 添加配置回归测试，拒绝范围输入中不受支持的 `%f`、`%.0f` 等占位符。

### 2. 改善小镇规则填写引导

- [x] 调整小镇申请界面，将小镇简介填写移动至规则填写前一页。
- [x] 将小镇菜单中小镇详情界面的“修改简介和规则”按钮拆分为“修改简介”和“修改规则”两个按钮。
- [x] 改进小镇申请和修改规则功能：使用规则预览框展示已填写的规则，每条规则右侧提供“删除”按钮。
- [x] 在规则预览框底部增加规则输入文本框，并在文本框下方提供“添加规则”按钮。
- [x] 规则添加后立即显示在预览框中，并保持系统自动编号。

### 9. 移除投票界面物品图标

- [x] 投票列表、详情和确认页统一使用纯文本 `DialogBody`。
- [x] 将正文默认渲染方式改为纯文本。
- [x] 只有确实需要物品展示时才显式启用物品图标。

### 14. 移除申请批准界面物品图标

- [x] 管理员批准、拒绝和要求补件确认页统一使用纯文本正文。
- [x] 将所有确认页默认设为纯文本，图标改为显式启用。

## 建议实施顺序

1. 处理 10、16、17，解除当前建镇死循环。
2. 临时关闭 QuickShop 等额补贴并处理 22。
3. 修复服务台交互及爆炸保护：4、8。
4. 完成 Residence 传送链路：3、18、21。
5. 完成持久化草稿：12、13。
6. 处理玩家名显示：11、15、19、20。
7. 完成批量扩张和投票通知：6、7。
8. 集中处理 ESC、纯文本界面、Buff 和规则引导等显示问题。

## 验收要求

- [x] 所有数据库写入继续通过事务和幂等键保护。
- [x] 创建、退款、强制解锁、扩张和传送点修改均有审计记录。
- [x] 不通过直接编辑生产 SQLite 或 Residence YAML 修复业务状态。