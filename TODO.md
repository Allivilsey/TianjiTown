# `report.md` 修复 TODO

本清单覆盖报告中的 19 条反馈。标题中的 `Rxx` 对应报告原编号；实际执行顺序按“共享基础 → 规则编辑器 → 申请流程 → 成员/访客 → 领地扩张 → 总体验收”排列，避免后续页面重复返工。

## 实施约束

- [ ] 玩家可见文案统一放入 `tianjitown-paper/src/main/resources/messages.yml`；`TownUiController` 只负责传递占位符，不新增同义硬编码文案。
- [ ] 共享页面先修改渲染模型和消息键，再接入申请、小镇等调用方；不得复制两套规则编辑或身份翻译逻辑。
- [ ] 布局调整不得绕过现有规则版本校验、草稿持久化、扩张批次幂等与权限校验。
- [ ] 自动化测试通过后仍需在 Paper 客户端人工验收 Dialog 的真实位置、尺寸、图标和点击行为。

## 阶段 1：修复共享消息与成员展示基础

### 1. [R06] 修复申请状态消息键的双重 `dialog.` 前缀

- [x] 将 `ApplicationStatusText.messageKey(...)` 统一为相对键（如 `application.status.draft`），再由 `dialogText(...)` 添加一次 `dialog.`；申请摘要和管理员审核页都通过同一入口取状态文案。
- [x] 保留 `messages.yml` 中全部申请状态中文文案，并修改 `ApplicationStatusTextTest`，断言最终解析的是 `dialog.application.status.*`，且结果不含“缺少消息配置”或英文枚举名。
- [x] 搜索其他已经包含 `dialog.` 的参数是否又传给 `dialogText/dialogComponent`，一并消除同类双前缀问题。

### 2. [R11] 建立统一的成员身份汉化

- [x] 在 Paper 展示层增加穷尽的 `MemberRole` 文案映射：`MAYOR → 镇长`、`DEPUTY_MAYOR → 副镇长`、`MEMBER → 成员`，文案键放入 `messages.yml`。
- [x] 小镇详情进入的只读成员列表统一调用该映射，不再直接输出 `member.role()`。
- [ ] 添加映射测试，保证新增枚举值时必须补充中文文案，所有玩家页面都不泄露 `MAYOR/DEPUTY_MAYOR/MEMBER`。

### 3. [R12] 统一成员排序并在排序后分页

- [x] 定义稳定顺序：镇长、每位副镇长、普通成员；同身份内按玩家 ID（Minecraft 玩家名）忽略大小写排序，无可用玩家名时回退 UUID 字符串，最后再以 UUID 作为稳定并列项。
- [x] 调整成员读取/展示流程，必须先对完整成员集合执行上述排序再分页；不能继续使用当前 `joined_at, player_uuid` 的数据库分页，也不能只在每一页内部排序。
- [ ] 让只读成员列表和成员管理列表复用同一排序结果；扩充仓储或独立排序器测试，覆盖身份混排、大小写、同名回退及跨页边界。

### 4. [R14] 将身份汉化应用到成员管理全流程

- [x] 在成员管理列表、成员详情、“你的身份”、调整身份确认页和调整成功提示中使用阶段 1 的统一中文映射。
- [x] 保留内部动作参数和数据库枚举值为英文枚举，不把显示文案反向用于业务判断。
- [ ] 验证镇长、副镇长和成员三种身份在列表、详情及变更前后均显示一致。

## 阶段 2：重构共享规则编辑器

### 5. [R01] 将建镇申请说明改为面向玩家的操作说明

- [x] 审核 `dialog.application.*-guidance`、规则输入提示及按钮 tooltip，删除“持久化、刷新、页面版本”等开发语气，只说明玩家需要填写什么、限制是什么、下一步会发生什么。
- [x] 建议基础资料说明改为“填写小镇名称、代码和简介；代码由 1～12 个英文字母组成，并作为小镇领地名。”，规则页说明改为“请添加 1～50 条需要小镇成员遵守的规则。”。
- [ ] 人工检查三个申请步骤，确保说明简短、无重复且不与当前按钮行为冲突。

### 6. [R02] 缩小“添加规则”按钮并清理过时说明

- [x] 从 `application.content-guidance` 删除“每行视为同一条规则，提交前可返回修改简介”；当前规则是一条一条添加，不能再描述旧的多行输入模式。
- [x] 将 `RuleEditorDialogRenderer.ADD_WIDTH` 从整行宽按钮改为普通文本按钮宽度，并为长语言文本保留合理最小宽度；申请和小镇编辑器共用该规格。
- [x] 在渲染模型测试中断言添加按钮不再占满“预览宽度 + 删除宽度”。

### 7. [R03] 禁止预览按钮触发等待页，并改造删除按钮

- [x] 将规则编辑 Dialog 的 `afterAction` 从 `WAIT_FOR_RESPONSE` 改为 `NONE`；预览控件保持无业务动作，点击后仍停留在当前编辑页，添加和删除回调完成后主动刷新页面。
- [x] 删除按钮改为与高度匹配的正方形窄按钮，正文只显示 `minecraft:block/barrier` Sprite，不显示“删除第 N 条”等文字；具体规则编号和删除后果放入 tooltip。
- [x] 保留删除目标携带的页面版本、规则索引和预期文本校验，避免样式调整引入过期页面误删。
- [ ] 增加布局/行为测试，并在客户端连续点击预览按钮，确认不会出现“正在等待服务器响应”。

### 8. [R05] 删除说明区中重复的规则预览

- [x] `ruleEditorPreview(...)` 的正文只保留页面标题和必要说明，不再逐条拼接规则；每条规则只在可操作的预览行中出现一次。
- [x] 空规则时保留唯一的“暂未设置规则”提示，避免正文和按钮区各显示一份空状态。
- [ ] 测试 0、1、50 条规则时，每条规则正文在界面模型中只出现一次。

### 9. [R04] 将新增文本框放在规则预览与添加按钮之间

- [ ] 为共享规则编辑器建立明确的纵向布局契约：标题/说明 → 规则预览行及对应删除控件 → `rule_text` 输入框 → 普通尺寸“添加规则”按钮 → 页面级动作。
- [ ] 不再依赖当前 `DialogBase.inputs + MultiAction` 的默认分区顺序；若当前 Paper Dialog 不能在同页将输入插入动作列表，应把规则行改为专用只读主体/交互控件组合，或将批量页面动作拆到子 Dialog，但最终客户端顺序必须符合上述契约。
- [ ] 在实际 Paper 客户端验证输入框位于最后一条规则之后、添加按钮之前；长规则和 50 条规则时也不能错位。

### 10. [R13] 小镇规则编辑复用申请规则编辑器

- [x] 在 R02～R05 的共享布局稳定后，让 `openTownRuleEditor(...)` 与申请第 2 步调用同一个渲染入口、按钮规格和消息键，不保留小镇专用的旧排列。
- [x] 仅注入不同的保存回调：申请场景更新 `ApplicationFormSession` 并持久化草稿，小镇场景继续使用版本号更新正式规则。
- [ ] 对两处界面执行截图式人工对比：除标题、返回路径和页面级动作外，预览、删除、输入、添加的设计完全一致。

## 阶段 3：修整建镇申请、邀请与审核结果

### 11. [R07] 调整初始成员邀请文案

- [x] 将 `dialog.invitation.message` 改为 `{player}邀请你加入{town}`（按项目标点规范决定是否在末尾加句号）。
- [x] `sendInitialMemberReminder(...)` 同时传入申请人显示名和小镇名；申请人名称不可用时使用既有 `displayName(UUID)` 回退策略。
- [ ] 增加占位符测试，确认首次邀请和冷却后的再次提醒都显示相同的“玩家名 + 小镇名”文案。

### 12. [R08] 精简管理员的小镇申请审核摘要

- [x] 从 `openAdminApplication(...)` 移除玩家 UUID、申请编号、小镇代码、申请费用途说明，以及两名初始成员的确认状态行。
- [x] 将“Residence 领地名”改为中性的“小镇领地名”或“领地名”，只保留规范化后的实际领地名；不要同时显示代码和领地名两个同义字段。
- [x] 保留审核需要的申请人名称、申请状态、提交/更新时间、小镇名称、简介、规则、选址和创建错误；批准、拒绝、补件及预览动作不变。
- [ ] 添加审核摘要模型测试，断言禁止字段和 `Residence` 品牌词不再出现。

### 13. [R09] 替换“申请已提交”说明文本

- [x] 将 `dialog.notice.application-submitted-message` 精确改为“少女审核中...”，标题和“查看申请”后续动作保持不变。
- [x] 增加消息测试，防止旧的“管理员审核期间仍可撤回”说明再次出现。

### 14. [R10] 替换“小镇创建成功”说明文本

- [x] 将管理员批准成功后的正文迁入 `messages.yml`，精确显示“小镇创建成功。”；移除当前“小镇与 Residence 领地已经创建完成。”硬编码。
- [x] 成功标题与失败/超时分支分开配置，失败详情、恢复动作和幂等处理保持原逻辑。
- [ ] 覆盖成功、失败、30 秒超时后三条回调路径，确保只有成功页使用指定正文。

## 阶段 4：精简访客页面

### 15. [R15] 访客管理说明只显示当前访客数量

- [x] `openVisitorCenter(...)` 的顶部说明仅保留“当前访客: {count}”，删除 Residence 权限说明和成员权限范围说明。
- [x] 数量改用 `messages.yml` 占位符文案；访客列表、邀请入口和权限判断不变。

### 16. [R16] 移除邀请访客空状态中的排除说明

- [x] 从邀请访客页面删除“本镇成员和已有访客不会显示”，尤其是无候选玩家时的空状态说明。
- [x] 候选人过滤逻辑仍须排除本镇成员和已有访客；本条只改玩家可见说明，不改变资格规则。
- [x] 检查 `tooltip.visitor.invite` 与候选条目 tooltip，避免同一句排除说明以其他形式重复出现。

## 阶段 5：修复小镇领地扩张界面

### 17. [R17] 更改扩张界面标题

- [x] 将 `dialog.territory.title` 精确改为“小镇领地扩张”，只在说明/图例中保留 5×5 地图概念。
- [x] 检查入口 tooltip 和返回路径标题，避免仍出现“5×5 领地扩张地图”旧名称。

### 18. [R18] 将确认与清除恢复为正常文本按钮

- [x] `TerritoryDialogRenderer` 的“确认扩张所选区域”和“清除已选区域”使用可读文本作为按钮正文，不再用绿色/橙色方块 Sprite 代替文字。
- [ ] 将 25 个地图格与批次操作按钮分离为独立布局区，避免宽文本按钮重新拉开五列地图；若 Paper 单个 `MultiAction` 无法提供独立操作区，则使用批次操作子 Dialog 承载两个正常文本按钮，不得回退为无文字图标。
- [x] 保留确认按钮的总价/数量 tooltip、清除后的选区与幂等键清理，以及扩张批次整体提交逻辑。
- [ ] 更新 `TerritoryDialogRendererTest`：地图始终为 25 个 20px 格子，批次操作为文本按钮且不参与地图列宽计算。

### 19. [R19] 修复选中格子的缺失消息键

- [x] 统一代码和配置路径：推荐把 `selected` 移到 `dialog.territory.cell.selected`，与 `TerritoryDialogRenderer.tooltip(...)` 当前读取路径保持一致，并移除或迁移旧的 `dialog.territory.selected`。
- [x] 将该键加入必要消息校验或针对性测试，断言选中可扩张区域的 tooltip 显示“已选中（再次点击取消）”，不显示“缺少消息配置”。
- [ ] 人工验证选择、取消、清空和重新打开页面四种状态，确保选中提示只出现在已选的可扩张格子上。

## 阶段 6：最终验证

- [x] 运行项目完整测试（`mvn test`），重点确认 `ApplicationStatusTextTest`、`PluginMessagesTest`、`RuleEditorDialogRendererTest`、成员排序测试和 `TerritoryDialogRendererTest`。
- [ ] 在测试服按 R01～R19 逐条回归，并至少使用 0/1/多条规则、三种成员身份、跨页成员、0/多名访客、0/1/多个扩张选区进行验证。
- [x] 搜索玩家可见内容中的 `Residence`、`MAYOR`、`DEPUTY_MAYOR`、`MEMBER`、“缺少消息配置”和旧标题/旧说明，确认没有本轮反馈所指的残留。
- [x] 本轮不修改数据库业务数据；若为玩家 ID 排序新增持久化名称字段，必须单独评审迁移和旧数据回填后再实施。
