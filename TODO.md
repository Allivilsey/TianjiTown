## 类职责拆分（2026-09-05）

根据生产 Java 源码的结构检查整理。按职责边界拆分，行数仅作参考；以下职责拆分均已实施，并通过全模块验证。

### 第一批：边界清晰，优先实施

- [x] **SitePolicy**：保留选址、世界边界与领地冲突校验；将传送、粒子绘制、预览定时任务和玩家预览会话提取到 `TerritoryPreviewService`。保留玩家退出及插件关闭时的预览清理。
- [x] **ServiceStationController**：提取 `StationRegistry` 管理登记、查询及配置序列化；提取 `HandbookService` 管理手册制作、发放和冷却；将方块保护事件提取到独立监听器。控制器保留创建、移除和交互流程，统一校验讲台标记与登记记录。
- [x] **TownActions**：按业务提取申请、成员、治理、经济、扩张和 Buff 操作；统一写入拦截、异步执行、错误映射及结果转换。原入口可暂作兼容门面，保留成员变更后的领地同步和 Buff 刷新，以及解散流程的阶段顺序。

### 高优先级：启动与存储职责

- [x] **TianjiTownPlugin**：提取启动协调器、生命周期任务调度器和组件注册器；分离依赖检查、数据库启动、组件注册及周期任务注册。统一启动失败与关闭清理，保持生命周期代次检查、过期回调拦截和任务回收的一致性。
- [x] **TownRepository**：优先提取 `TownDeletionStore`，集中删除归档、成员快照、关联记录处理和资源释放；随后提取资料修改、查询及审计职责，保留仓储门面。关联 SQL 必须继续运行于原有事务中，保持先归档、确认 Residence 移除后再释放资源的顺序。

### 后续拆分

- [x] **TownUiLegacyFacade**：将功能 Dialog 的装配移到 `TownUiController`；逐步让功能类直接依赖所需的导航、展示和结果处理服务，缩小对整个 Facade 的依赖，避免只增加转发层。同步调整 `TownUiStructureTest` 中要求功能实现通过 Facade 交互的结构约束。
- [x] **ResidenceLandProtectionService**：提取权限同步、区域几何校验和投影修复协调职责；统一维护主线程约束与内部变更标记，保留受控所有权、碰撞检查及重建失败清理。
- [x] **TownGovernanceDialogs**：按镇规编辑、镇长转让和投票拆分；将通用规则编辑控件从治理页面提取，保留规则版本冲突处理和通知回调。
- [x] **TownBonusRepository**：优先将 `diagnose()` 及相关诊断查询提取到独立诊断仓储；系统数据库、账本、税收和扩张状态查询不应由加成仓储承担。
- [x] **TerritoryExpansionStore**：提取批量扩张 Store，单次与批量流程共享接收同一 `Connection` 的 SQL 辅助逻辑；保持准备、完成和退款流程的事务原子性及业务键幂等性。

### 实施与验证约束

- 每次围绕一个职责边界拆分，复用已有组件，避免仅为降低行数增加空转发类。
- 各项实施后运行对应模块的行为测试；重点覆盖线程切换、生命周期清理、事务回滚、重复请求及失败恢复。
- UI 调整需保留路由、会话失效及权限行为，并同步更新结构测试；行数阈值不能替代行为验证。
- `TownRuntime` 与 `VaultSettlementService` 暂不优先拆分：前者已大量委托子运行时，后者的转账与补偿流程内聚性较强。

### 本次验证（2026-09-05）

- `mvn -B clean verify`：所有模块通过，生成 `tianjitown-paper/target/TianjiTown-1.4.0.jar`。
- 新增生命周期过期回调、异步关闭、预览替换与清理、服务台登记与标记一致性、诊断存储故障映射测试。
- 既有事务回滚、重复扩张与退款、删除归档、Residence 修复、UI 路由与会话测试全部通过。
