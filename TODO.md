# TODO

## 管理员小镇经济分析命令（待实现）

目标：让管理员按时间调查全服及单个小镇的税收、捐款和玩家收入，并从汇总追查到具体交易。只读查询，支持游戏内和控制台。

### 命令与交互

统一入口 `/tianjitown economy`，无参数显示帮助。新增权限 `tianjitown.admin.economy`，默认 OP 可用，沿用 `tianjitown.admin` 总权限；命令执行、帮助和补全使用同一权限规则。

| 命令 | 用途 |
| --- | --- |
| `economy overview [筛选参数]` | 全服总览：税收、捐款、补贴、已记录玩家税前收入/已扣税/税后收入、交易笔数和收款人数 |
| `economy town <小镇全名或UUID> [筛选参数]` | 单镇收入结构、按来源拆分、每日趋势、纳税与捐款贡献排名 |
| `economy player <玩家名或UUID> [筛选参数]` | 玩家按来源、交易时所属小镇拆分的收入、纳税、捐款和交易明细 |
| `economy top <tax\|donation\|income> [筛选参数]` | tax/donation 按小镇排名，income 按玩家税前收入排名；可指定 `--town` 限定玩家范围 |
| `economy transactions <tax\|donation\|income> [筛选参数]` | 分页调查明细，支持 `--town`、`--player`，展示交易关联标识便于追查 |

以上子命令均带 `/tianjitown` 前缀。小镇全名沿用现有名称解析习惯，支持带空格名称；UUID 用于同名历史小镇和离线玩家的准确定位。玩家名仅查本地身份记录，重名或无法唯一匹配时提示使用 UUID。

- 时间：默认最近 7 天；`--period today|7d|30d|all` 或 `--from YYYY-MM-DD --to YYYY-MM-DD`，两种写法互斥。日期范围包含结束日期，实现采用 `[开始日零点, 结束日次日零点)`；统一 `TownTime.ZONE`（Asia/Shanghai）。7d/30d 为截至查询时刻的最近 7/30 个自然日，含今天。每份结果显示精确起止时间、时区和查询截止时刻。
- 来源：`--source quickshop|jobs|globalmarketplus|all` 适用于税收/玩家收入；捐款查询拒绝来源筛选。总览带来源筛选时，明确该筛选仅作用于税收、关联补贴和玩家收入，捐款仍为区间总额。
- 分页：`--page` 默认 1，`--limit` 默认 10、最大 50；排行按金额降序、UUID 稳定排序，明细按发生时间和唯一 ID 倒序。下一页保留筛选条件与查询截止时刻。
- 展示：总览保持简短，提供查看小镇、玩家和明细的点击入口；控制台输出等价文本。金额沿用现有币种精度和格式，所有文本放入 `messages.yml`。非法日期、负页码、无效组合和未找到对象给出明确提示；空结果展示“该范围无记录”。

### 统计口径

| 指标 | 定义与边界 |
| --- | --- |
| 小镇税收收入 | 已落账 `QUICKSHOP_TAX`、`JOBS_TAX`、`GLOBALMARKETPLUS_TAX` 的金额，按来源拆分 |
| 小镇捐款收入 | 已落账 `DONATION` 金额；同时显示捐款笔数、去重捐款人数和玩家贡献 |
| 服务器补贴 | `SERVER_TAX_SUBSIDY` 单独展示，按关联业务键识别来源；QuickShop 以实际到账金额为准，不按税额推算，可能因限额只有部分补贴或无补贴 |
| 本报告小镇收入合计 | 税收 + 捐款 + 服务器补贴；不包含管理员调账、退款等其他入账，不等于余额或完整资金净变化 |
| 玩家税前收入 | 已接入来源确认成功的收款金额；QuickShop SELLING 归店主、BUYING 归出售物品的交互玩家，不能统一记到店主名下 |
| 玩家已扣税/税后收入 | 实际扣税及税前收入减实际扣税；未征税的成功收入税额为 0，未知结算结果不推算到账 |

- 玩家收入是收款流水，不是利润、余额增量或服务器货币增发；不扣商品成本，不把捐款支出混入税后收入。玩家间交易、税款与捐款属于资金转移，不能将玩家收入和小镇收入相加作为“全服经济总收入”。
- 税收/捐款按账本入账时间归属区间；玩家收入按交易实际发生时间归属区间。展示两种时间，解释异步入账跨日造成的差异；核对使用业务键，不强求同一日汇总恒等。
- 税收归属当时受益小镇；玩家收入归属交易时的成员小镇，未入镇单独归类。换镇、退镇、改名不改写历史；默认包含已归档小镇并标注状态，不按当前成员表重新归集。
- 主汇总仅包含确认成功的记录；待处理、失败、补偿中记录单独提示，不提前算作收入。退款/冲正如需接入，记录独立关联事件并分别展示原收入、冲减和净额，不能删除原交易或把补偿退款再算为收入。

### 数据基础与采集补全

现状：已有 `ledger_entries`、`economy_operations`、`quickshop_tax_records`、`external_income_tax_records`，足以建立已落账税收和捐款分析。现有税记录只保存正税额交易，不能代表全部玩家收入；直接从税额除以税率还原收入也不可靠。

- [ ] 小镇收入口径以 `ledger_entries` 为唯一汇总来源，税记录用于交易详情和交叉核对，禁止把账本和税记录重复累加；捐款关联 `economy_operations` 追查状态，未完成操作不计入。
- [ ] 新增统一玩家收入记录（建议 `player_income_records`）：唯一 ID、来源、来源业务键、收款玩家 UUID/名称快照、交易时小镇 UUID/名称快照（可空）、税前金额、实际扣税、结算状态、发生时间、记录时间及税记录关联信息。金额使用现有最小货币单位整数，校验非负、税额不超过收入，汇总处理溢出。
- [ ] 将收入观察与征税条件分开：QuickShop、Jobs、GlobalMarketPlus 的成功收入，即使零税率、税额舍入为零、未入镇或关闭征税，也应记录；采集不改变交易、税率或清算行为。
- [ ] 每个适配器明确“成功收款”的可确认时点，覆盖事件取消、支付失败、税款补偿和其他插件修改金额的情况；不能确认最终到账时标记为待确认或覆盖受限，禁止把支付前事件直接当成成功收入。
- [ ] 使用来源 + 稳定交易键保证去重；无原生交易 ID 的来源在首次接收时生成并贯穿后续处理和重试，不能重试时重新生成。征税、收入记录和账本建立关联；数据库内相关写入尽量原子完成，外部结算与数据库之间明确重试和故障恢复边界。
- [ ] 报告固定显示“已接入来源的已记录收入”，列出来源启用/可用状态、采集起点及已知故障或缺失区间。未安装、停用、停服或采集失败不等于收入为零；不承诺包含其他插件奖励、管理员发钱或所有 Vault 余额变动。
- [ ] 不回填既有开发数据。完整玩家收入采集前的税记录如提供查询，只能单列“已征税交易收入（不完整）”，不能与新收入表直接合并为全量收入。跨采集起点的报告明确标记覆盖不完整。

### 实现拆分与查询性能

- [ ] storage：新增经济分析只读查询仓储、筛选对象和汇总/排行/明细结果；SQL 聚合、参数绑定和分页，按来源/时间、小镇/时间、玩家/时间建立必要索引。核对现有业务键关联，避免一笔税款关联补贴后放大金额。
- [ ] integrations/runtime：补齐上述玩家收入采集、持久化关联和覆盖状态；沿用线程约束，不在异步线程调用要求主线程执行的 Bukkit/Vault API。
- [ ] paper：新增管理员经济分析命令类，接入权限映射、帮助、补全和消息配置；数据库读取通过现有异步读取机制执行。同一份汇总使用一致性读取快照，避免多个指标读取到不同结算状态。
- [ ] 大范围查询设置并发限制和超时，禁止把全部流水加载到主线程；`all` 查询同样走数据库聚合。页面固定截止时刻，说明迟到入账仍可能影响再次查询结果，不宣称跨请求完全冻结。
- [ ] 如需新增表或索引，直接维护 `tianjitown-storage/src/main/resources/db/migration/V1_0__initial_schema.sql`；不新增历史迁移、配置版本门禁、旧库兼容或数据回填，不自动删除/重建用户数据库；项目版本保持 `1.0.0-SNAPSHOT`。

### 验收

- [ ] 固定样例：玩家成功税前收入 1,000、实际税款 50、成功捐款 200、实际补贴 30，则显示玩家税后收入 950，小镇税收 50、捐款 200、补贴 30、本报告收入合计 280；管理员调账不影响这些指标。
- [ ] 覆盖三个来源、QuickShop 两种交易方向、部分/零补贴、零税率和未入镇玩家；收入只归实际收款玩家，成功未征税收入仍进入玩家统计。
- [ ] 覆盖重复回调/重试、取消/失败、补偿和延迟落账，确保不重算、不虚报成功；税额明细与对应账本可通过业务键核对。
- [ ] 覆盖跨日/月时间边界、换镇/退镇/改名、归档同名小镇、离线 UUID、空数据及来源缺失；历史归属与覆盖提示正确。
- [ ] 验证管理员/分权限/无权限访问、控制台输出、过滤组合、稳定分页和大数据量异步查询；补充命令测试、SQLite 聚合测试及 README 使用示例。
