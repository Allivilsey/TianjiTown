# TianjiTown `report.md` 修复 TODO

本清单只覆盖 `report.md` 当前列出的 20 条反馈。编号 `R01`～`R20` 与报告原编号一一对应；执行顺序按安全性和代码依赖重新排列，不按报告顺序机械实施。

## 实施原则

- 所有玩家可见文案优先放入 `tianjitown-paper/src/main/resources/messages.yml`，避免继续在 `TownUiController` 中增加硬编码文本。
- 本轮均为事件保护、Dialog 布局与显示调整，不需要数据库迁移，也不修改现有申请、成员、资金或领地业务数据。
- 先完成全局导航和共用渲染器，再改具体页面，避免同一页面因 R04、R09、R10、R15、R16 被重复返工。
- “返回按钮”只负责页面导航；ESC 仍只关闭当前 Dialog，不触发保存、放弃或返回动作。
- 每完成一个阶段先运行 `mvn test`，再按文末人工验收矩阵在测试服验证。

## 阶段 1：先封堵服务台破坏入口

### R01. 小镇服务台左键与副手防破坏

- [ ] 将“是否为有效服务台”的判断统一收口到严格校验：方块必须是讲台、PDC 服务台 ID 非空、配置中存在相同 ID，且世界与坐标完全一致；复制的 PDC 标记不获得保护。
- [ ] 在 `PlayerInteractEvent` 中优先处理有效服务台：主手和副手的左键、右键都先取消原版方块/物品交互；只有主手右键打开一次小镇菜单，副手事件不得再次打开菜单或触发讲台原版行为。
- [ ] 保留“管理员潜行左键拆除”这一显式管理入口，并限定为主手单次执行，防止主副手事件造成重复注销或重复掉落。
- [ ] 将 `BlockBreakEvent` 从“破坏后注销记录”改为最终保护屏障：任何普通破坏事件命中有效服务台都必须取消，不能再通过生存、创造、快速挖掘或其他插件直接破坏；管理拆除继续走 `destroyStation`，不要依赖自然破坏事件清理配置。
- [ ] 保留现有爆炸和活塞保护，复核它们与新的严格服务台校验使用同一判定函数。
- [ ] 增加事件策略测试，并在测试服分别验证主手/副手、左键/右键、Residence 允许/拒绝、普通玩家/管理员潜行等组合；预期只有主手右键开菜单和管理员潜行主手左键拆除有效。

## 阶段 2：先建立全插件统一导航规则

### R16. 移除子页面内的“返回……”动作，将底部“关闭”改为“返回”

- [x] 为 Dialog 路由增加明确的父页面信息，不再依赖按钮名称猜测父级；至少登记主菜单、公共资产、成员管理、申请摘要、申请各步骤、小镇详情、规则、成员、访客、投票、账本、Buff 和领地扩张的父子关系。
- [x] 明确一级入口：玩家主菜单以及无上级页面的强制通知/邀请保留底部“关闭”；所有从一级入口进入的子页面，底部退出动作显示“返回”并导航到登记的父页面。
- [x] 改造 `openMenu`：由调用方显式传入父路由，并从各页面构造代码中删除原有“返回***”菜单项；父路由统一生成 Dialog 的底部返回动作，页面主体不再重复显示返回按钮。
- [x] 改造 `openDialogPage`、`openNotice`、确认框及输入表单，使调用方显式传入父路由；不能继续默认把底部退出动作写死为 `CLOSE`。
- [x] 保持 `canCloseWithEscape(true)`，ESC 只关闭并销毁会话，不执行底部返回动作，也不隐式保存或放弃表单。
- [x] 让当前未实际使用的 `returnButton` 接受显式父路由并成为唯一的底部返回生成器，删除依赖按钮显示名称的 `isBackButton`，确保导航不受文案变化影响。
- [x] 增加路由/按钮模型测试：一级页面底部为“关闭”，子页面底部为“返回”，子页面动作区不存在“返回主菜单/返回公共资产/返回小镇详情”等重复按钮；另验证 ESC 与点击返回行为分离。

## 阶段 3：整理成员入口与小镇详情

### R14. “成员治理”统一更名为“成员管理”

- [x] 将主菜单入口、中心页标题、页面摘要、访客页返回文案、成员详情标题和 `messages.yml` 中所有玩家可见的“成员治理”统一改为“成员管理”。
- [x] 同步修改玩家指南和功能文档中的界面路径，避免文档仍指向旧名称。
- [x] 内部治理领域类（投票、`MemberGovernanceSnapshot` 等）无需为显示改名，避免把纯文案修复扩大为无关重构。

### R08. 小镇详情改用只读文本成员列表

- [x] 将小镇详情页的成员入口改为独立动作（如 `TOWN_MEMBER_OVERVIEW`），不得再复用成员管理页的 `MEMBERS` 动作。
- [x] 新增只读成员概览，按“小镇规则”页面的纯文本 `DialogBody.plainMessage` 风格显示成员名、身份和必要的分页信息；成员条目不使用物品图标、不可点击，也不出现任命、移除或投票操作。
- [x] 成员较多时继续分页，但上一页/下一页是唯一的页面动作，返回由 R16 的底部返回动作负责。
- [x] 成员管理中心继续使用可操作的成员列表，两条入口在动作名、渲染方法和权限行为上保持隔离。

### R15. 成员管理内移除“返回小镇详情”

- [x] 从可操作成员列表中删除“返回小镇详情”菜单项。
- [x] 将该列表的父路由设为“成员管理”，底部返回应回到成员管理中心，而不是小镇详情；成员详情的父路由仍为可操作成员列表并保留当前页码。
- [x] 验证从“小镇详情 → 只读成员概览”和“成员管理 → 可操作成员列表”进入后，返回链路互不串页。

### R11. 小镇详情移除小镇代码和小镇状态

- [x] 从 `openTown` 的摘要中删除“小镇代码”和“小镇状态”两行。
- [x] 保留小镇名称、简介、规则数和领地名等仍有玩家价值的信息，不改变 `TownSnapshot` 或数据库字段。

### R12. 小镇详情领地名文案改为“小镇领地名”

- [x] 将 `Residence 领地名: *` 改为 `小镇领地名: *`，并把该文案迁入 `messages.yml`。
- [x] 搜索玩家 UI 中同类 `Residence` 技术名称，仅统一同一含义的展示文案；管理员诊断和日志仍可保留 Residence 原名。

### R13. 设置领地传送点提示移除 Residence 管理说明

- [x] 删除按钮悬浮提示中的“传送冷却和费用继续由 Residence 管理”，只保留“必须站在本镇领地内的安全位置”。
- [x] 不修改实际传送点校验、Residence API 调用、冷却或费用行为。

## 阶段 4：先做共享规则编辑器，再接入两个业务入口

### R09. 小镇详情的修改规则页将删除按钮放到对应规则右侧

- [x] 抽取 `RuleEditorDialogRenderer`（或等价的共享渲染方法），每条规则固定渲染为一行两列：左侧为只读规则预览，右侧为该规则的“删除”按钮；删除目标同时携带页面版本、规则索引和预期文本。
- [x] 规则输入框和“添加规则”放在规则列表之后；“返回”交给 R16 的底部导航，不再混入规则按钮网格。
- [x] 删除或添加成功后重新读取/刷新页面；提交时仍使用小镇版本号与现有 `updateTownProfile` 冲突校验，不能因布局调整绕过并发保护。
- [x] 保留至少一条、最多 50 条、单条长度等现有规则校验；删除前同时校验版本、索引和预期文本，页面打开后规则已变化时提示刷新，禁止误删同位置的新规则。
- [x] 增加渲染模型测试，断言每条规则后紧跟且只对应一个删除动作，奇偶条数和长文本都不打乱两列关系。

### R10. 建镇申请规则编辑复用小镇规则修改界面

- [x] 用 R09 的共享规则编辑器替换申请第 2 步当前“多行文本一次编辑全部规则”的实现，使预览、逐条删除、底部输入、添加规则的布局和小镇详情完全一致。
- [x] 申请场景的添加/删除只更新当前 `ApplicationFormSession`，每次变化后持久化到 `application_form_drafts` 的第 2 步，再刷新规则编辑页；不得提前创建或更新正式申请。
- [x] “上一步”“下一步”“保存草稿”继续保存当前规则集合；下一步时执行规则完整性校验，草稿保存允许暂时不完整。
- [x] 小镇资料编辑与建镇申请分别注入保存回调、权限和校验上下文，共用渲染布局，不复制两套按钮排列代码。
- [x] 验证添加、删除、返回再进入、保存草稿后重登恢复、正式提交五条路径的规则顺序与自动编号一致。

## 阶段 5：修整建镇申请表单与摘要

### R02. 统一小镇简介、名称和代码输入框宽度

- [x] 将申请基础资料页 `town_name`、`residence_name`、`description` 的输入宽度统一为 400；多行选项只影响简介高度，不再改变横向宽度。
- [x] 同时检查编辑简介页的宽度，确保同一字段在新建和修改场景下视觉一致。
- [x] 增加表单规格测试或抽取输入规格常量，防止三个宽度再次分叉。

### R03. 保存草稿提示改为固定文案

- [x] 将保存后提示正文改为准确的“已保存小镇申请草稿”，移除“第 * 步”和“当前全部输入”的动态描述。
- [x] 将标题和正文放入 `messages.yml`，`persistApplicationForm(..., exitAfterSave=true)` 只引用配置键。
- [x] 统一第 1、2、3 步的保存草稿出口，确保都显示同一提示；正式“完成资料并发送邀请”的提示不受影响。

### R04. 初始成员页底部放置“保存草稿”和“放弃草稿”，替换关闭按钮

- [x] 为初始成员页使用专用的双列底部动作区：成员选择和“完成资料并发送邀请”在主体动作区，“保存草稿并退出”“放弃草稿”按此顺序作为最后一行固定在底部。
- [x] 该页面不再生成通用“关闭”按钮；ESC 仍只关闭页面且不等同于放弃草稿。
- [x] 保持“保存草稿”允许成员未选全并持久化第 3 步；“放弃草稿”删除持久化草稿和内存会话后返回主菜单，不改变两者现有业务语义。
- [x] 验证候选玩家数量变化和长玩家名不会让底部两个按钮混入成员选择按钮之间。

### R05. 申请摘要移除内部/冗余字段

- [x] 从申请摘要正文移除：申请人 UUID、申请编号、提交时间、更新时间、建镇申请费及其状态。
- [x] 保留申请人玩家名、申请状态、名称、小镇领地名、简介、初始成员确认状态、规则、选址中心和管理员意见/自动创建错误。
- [x] 管理员审核如果仍需要内部标识，应通过审计/日志或管理员专用信息获取，不把这些字段重新塞回玩家申请摘要。

### R06. “当前状态”改为“申请状态”并汉化枚举

- [x] 将摘要标签改为“申请状态”。
- [x] 在 `messages.yml` 为 `DRAFT`、`SITE_SELECTED`、`SUBMITTED`、`UNDER_REVIEW`、`NEED_CHANGES`、`APPROVED_PROVISIONING`、`ACTIVE`、`REJECTED`、`CANCELLED`、`PROVISION_FAILED` 提供中文名称。
- [x] 使用穷尽 `switch` 或集中映射方法渲染，禁止直接拼接 `application.status()`；新增枚举时应由编译失败或测试失败提醒补充翻译。
- [x] 增加状态映射测试，断言所有 `ApplicationStatus.values()` 都得到非空中文文案且页面不出现英文枚举值。

## 阶段 6：清理预览、资产与 Buff 页面

### R07. 领地预览不再发送粒子持续时间聊天提示

- [x] 申请领地预览和小镇领地预览统一使用 `SitePolicy` 的静默预览入口，不再发送“已显示 5×5 区块……粒子将持续约 15 秒。”。
- [x] 只移除成功后的 `preview-started` 消息；世界未加载、传送失败、跨世界等错误提示继续保留，传送成功提示是否显示保持现状。
- [x] 若 `chat.site.preview-started` 已无调用，删除该配置键及过时注释；粒子时长和定时任务本身不变。

### R17. 公共资产页移除税收适用范围说明

- [x] 从公共资产摘要中删除“适用于 QuickShop 实际收款、Jobs 工资与 GlobalMarketPlus 成交收入”和“普通转账、管理员调整及其他 Vault 变动不征税”两行。
- [x] 保留余额、税率、领地单元、QuickShop 补贴剩余额度与刷新时间等状态信息，不修改实际计税逻辑。

### R18. 速度/生命 Buff 参数页移除物品装饰

- [x] 将 `openBuffDurations` 的摘要从 `DialogBody.item` 改为纯文本 `DialogBody.plainMessage`（或现有 `dialogTextBody`），移除药水物品图标。
- [x] 保留 Buff 商店列表中的可点击类型入口图标；本条只影响进入“速度/生命”后的参数选择页。
- [x] 如果 `dialogBody(ItemStack)` 因此不再有调用，删除该方法，防止后续页面误用带物品装饰的正文。

### R19. Buff 滑动条显示标签和当前数值

- [x] 按税率滑动条的双占位符格式修改文案：持续时间使用 `&f%s: %s 周`，强度使用 `&f%s: 等级 %s`，让客户端同时渲染字段标签和当前滑块值。
- [x] 将范围格式校验从“至少一个 `%s`”收紧为“恰好两个未转义 `%s`”，并同时校验税率、Buff 周数和 Buff 强度格式。
- [x] 扩充 `PluginMessagesTest`：拒绝 `%f`、`%.0f`、缺少当前值占位符或多余占位符，接受双 `%s` 与转义 `%%`。
- [ ] 在测试服拖动两个滑块，验证 1～4 周和 I～V 对应的数值实时变化；提交仍使用响应中的整数值，价格计算不变。

## 阶段 7：最后修复领地扩张的刷新布局

### R20. 领地扩张选择后保持 5×5 密集排列

- [x] 修复 `TerritoryDialogRenderer` 的列宽污染：25 个地图格和追加的“确认/清除”宽按钮不能继续共用会按最大按钮宽度扩张的五列网格。
- [x] 优先将确认/清除渲染为独立底部动作区；若 Paper Dialog 类型无法分离动作区，则使用与 `CELL_SIZE` 相同宽度的固定图标按钮并用 tooltip 展示完整文案，保证五列宽度始终为 20px。
- [x] 无论是否已选择区域，前 25 个按钮的数量、顺序、宽度和五列结构必须完全一致；刷新只允许改变对应格子的选中 Sprite、tooltip 和汇总数值。
- [x] 保留选区集合、批次幂等键、总价、容量、连通性、碰撞校验与整体回滚逻辑，不用连续单格扩张替代批量提交。
- [ ] 抽取可测试的布局模型，断言 0、1、多个选中格时地图按钮规格不变，宽操作按钮不进入地图列；再在不同 GUI 缩放比例下人工确认点击前后不散开。

## 最终验收矩阵

- [ ] 服务台：普通玩家在主手/副手、左右键、生存/创造模式下均无法破坏；主手右键只打开一次菜单；管理员潜行主手左键可正常拆除且配置同步清理。
- [ ] 导航：从每个一级入口进入两层以上子页面，主体没有“返回***”按钮，底部显示“返回”；点击返回逐级回退，ESC 只关闭。
- [ ] 申请：三个输入框等宽；规则编辑布局一致；成员页底部按钮位置正确；保存草稿显示指定文案；摘要字段与全部状态翻译正确。
- [ ] 小镇：详情不显示代码/状态，领地名文案正确，传送点提示已精简；只读成员概览与成员管理功能完全分离。
- [ ] 规则：小镇与申请两处均为“规则预览在左、对应删除在右”，添加、删除、刷新、持久化和并发冲突处理正确。
- [ ] 资产/Buff：公共资产说明已移除；速度和生命参数页无物品装饰；两个滑块实时显示当前数值。
- [ ] 预览/扩张：领地预览没有持续时间聊天提示；扩张地图在选择、取消和清空后始终保持密集 5×5 排列。
- [ ] 自动化：执行项目完整测试；确认本轮没有新增数据库迁移，且 `report.md` 和用户已有的其他工作区改动未被覆盖。
