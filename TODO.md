# report.md 修改方案

依据：`report.md` 的 17 项反馈。本轮代码、配置和自动测试已实施；客户端交互、截图及完整测试服验收尚未完成。勾选项表示对应实现或自动验证完成，不代表人工验收通过。详见 `test/reports/auto/AUTO-R12_2026-09-05_21-34-03.md`。文件位置相对项目根目录；Java 类名可用于检索对应模块。

## 一、申请资料与草稿流程

### 1. 小镇代码改为 3～9 个英文字母（报告 1）

- [x] 将 core 模块 `ApplicationText.validate()` 的长度限制及错误参数由 1～12 改为 3～9，保留仅允许英文字母与统一小写的既有规则。
- [x] 将 `TownApplicationFormDialogs` 的代码输入框最大长度由 12 改为 9；同步 `messages.yml` 中申请说明、字段要求和相关文档。
- [x] 检查 `TownResidenceName`、存储读取及 Residence 集成对旧代码的依赖。新申请和代码修改执行新限制；已有 1～2 或 10～12 字母的小镇代码应仍可读取和管理，避免直接收紧底层正则导致旧小镇不可用。旧草稿重新提交时给出明确修改提示。
- [ ] 验收：3、9 位通过，空值、1、2、10 位及数字、空格、中文、符号被拒绝；大小写归一化及重名校验不退化；旧小镇正常加载。

### 2. 删除保存草稿后的多余提示界面（报告 2）

- [x] 调整 `TownApplicationDrafts` 中显示 `dialog.notice.form-draft-saved-message` 的分支，保存成功后直接退出编辑界面，匹配“保存草稿并退出”的按钮含义。
- [x] 保留实际保存动作、失败提示和可恢复编辑内容的能力；检查基础资料、规则编辑等全部保存入口。
- [x] 区分“已保存小镇申请草稿”的提示页与报告 4 的选址、初始成员引导，避免误删后续引导；确认无引用后再清理专用消息键。
- [ ] 验收：保存后不再出现多余确认页；重新进入及重登后内容一致；保存失败仍能看到原因并继续编辑。

### 3. 规则编辑页按钮等宽（报告 3）

- [x] 检查 `TownApplicationFormDialogs` 与 `RuleEditorDialogRenderer` 的页脚布局，让“保存草稿”和“下一步”使用同一宽度定义，核对上一页、末页等状态。
- [ ] 验收：不同规则数量、翻页状态及 GUI 缩放下两个按钮宽度一致，文字完整、点击区域正确。

### 4. 已选址时显示“重新选择领地”（报告 6）

- [x] 在 `TownApplicationDialogs` 等选址按钮入口，根据当前申请是否已有有效领地选择切换文案：未选址为“选择小镇领地”，已选址为“重新选择领地”。
- [x] 增加独立消息键，按钮标题与悬浮提示保持一致，沿用现有重新选址行为及权限判断。
- [ ] 验收：首次进入、选址成功、重新打开、重选成功及取消重选后的文字均与实际状态一致。

## 二、多行提示与确认按钮

### 5. 长提示显式换行（报告 4、5、7、17）

- [x] 修改 `tianjitown-paper/src/main/resources/messages.yml`，建议采用 YAML `|-` 多行字符串保持现有字符串类型；先检查 `PluginMessages` 及聊天、正文、悬浮提示渲染链是否保留换行，必要时逐行构造组件。
- [x] 使用下表文案，每一行显式保留原颜色；报告 4 按“小镇领地”统一措辞。

| 报告 | 消息键 | 目标文案（` / ` 表示换行） |
| --- | --- | --- |
| 4 | `application.draft-saved` | `&a申请草稿已保存` / `&a接下来请选择小镇领地` / `&a并等待两名初始成员确认` |
| 5 | `dialog.notice.application-cancelled-message` | `&f已撤回申请` / `&f你需要等待一段时间才能再次申请` |
| 7 | `dialog.confirmation.submit-application-consequence` | `&7提交后需等待管理员审核` / `&7批准时将从申请人余额扣除 {amount}` |
| 17 | `dialog.buff.confirm-consequence` | `&7强度: {level}` / `&7持续时间: {weeks} 周` / `&7价格 {price}` |

- [ ] 验收：实际分别显示 3、2、2、3 行，没有字面量 `\n`；颜色及金额等占位符正常，较大金额下无裁切。

### 6. 为三个确认操作使用独立文案（报告 8、12、14）

- [x] 检查 `ApplicationSubmissionDialogRenderer`、`TownMayorTransferDialogs`、`TownMembershipUi`/`TownMembershipDialogs` 与公共确认组件的传参方式，支持调用方显式传入确认标题和悬浮提示。
- [x] 为以下操作分别新增专用按钮标题键和 tooltip 键，不复用含义不明的通用确认提示；键名以下列建议为起点，实施时与现有命名规范保持一致。

| 报告 | 建议键前缀（位于 `dialog.confirmation`） | 按钮与悬浮提示文案 |
| --- | --- | --- |
| 8 | `submit-application-confirm` | 确认提交申请 |
| 12 | `accept-mayor-confirm` | 确认成为镇长 |
| 14 | `change-role-confirm` | 确认角色变更 |

- [x] 同步 `MessageContract` 及配置加载的兼容机制，确保缺少新增键的旧消息文件能获得默认值。
- [ ] 验收：分别自定义三个操作的 tooltip，确认仅影响对应界面；取消、返回、确认执行及重复点击保护保持正常。

### 7. 移除拒绝接任的关闭提示（报告 11）

- [x] 删除 `TownMayorTransferDialogs` 对 `dialog.tooltip.transfer.reject-close` 的引用，不再显示“本次请求将立即关闭”；无其他引用后删除默认键及对应契约。
- [ ] 验收：悬浮提示不再出现该句或空白占位行，拒绝动作仍正常关闭请求。

### 8. 角色变更后果显示玩家 ID（报告 13）

- [x] 将 `dialog.confirmation.change-role-consequence` 改为 `&7玩家 {player} 的角色将变更为 {role}`，在 `TownMembershipUi` 中传入目标玩家的游戏账号名及本地化角色名。
- [x] 玩家 ID 按游戏账号名处理，沿用项目离线姓名查询/缓存机制；无法解析时使用可识别的 UUID 回退，不能误显示操作者或装饰昵称。
- [x] 同步占位符契约并检查文本转义，避免玩家名称被解释为格式标记。
- [ ] 验收：在线、离线目标及姓名不可用时均能识别正确人员，角色名称和实际操作一致。

## 三、时间显示与税收补贴周期

### 9. 全面统一上海时间显示（报告 9）

- [x] 全量检索 UI、命令、聊天通知、诊断报告中的时间占位符、`Instant.toString()`、隐式字符串转换及默认系统时区用法。重点覆盖申请提交/审核、冷却、成员加入、镇长交接、投票、账单、补贴刷新及增益到期时间。
- [x] 建立公共显示格式化入口，固定使用 `ZoneId.of("Asia/Shanghai")` 与 `yyyy-MM-dd HH:mm:ss`；显示层统一调用，不再直接展示 `T…Z` 格式。
- [x] 时间戳继续按 `Instant`/epoch 存储和比较，仅在展示时转为上海时间；剩余时长沿用时长格式，空时间保留“尚未提交”等状态文案。
- [x] 检查 `TownAdminApplicationDialogs` 及相关展示辅助类的占位符传参，处理直接传入 `Instant` 后被消息系统自动转成 UTC 字符串的情况。
- [ ] 验收：固定输入 `2026-09-05T12:16:55.617Z` 显示为 `2026-09-05 20:16:55`；在服务器默认时区为 UTC 时结果仍相同；覆盖跨日、跨年、空值和倒计时，过期判断不受影响。

### 10. 税收补贴周一 04:00、每日 04:00/16:00 刷新（报告 15）

- [x] 修改 storage 模块 `EconomyTaxStore` 的周期计算（当前十二小时窗口按 00:00/12:00 划分），检查 `TownTaxRuntime` 传入时区并固定业务口径为上海时间。
- [x] 十二小时窗口按 `[04:00,16:00)` 与 `[16:00,次日04:00)` 划分；周窗口按 `[周一04:00,下周一04:00)` 划分。建议以本地时间减 4 小时后计算日期/半日边界，再加回 4 小时，避免凌晨归错周期。
- [x] 配额查询、预留、结算、重试补发及 `nextWeek`/下一次十二小时刷新时间共用同一周期逻辑；不以服务器启动时刻作为起点。
- [x] 核查既有补贴记录的 `week_start`、`period_12h_start` 与状态语义，制定旧记录周期归属的兼容或迁移步骤：按明确的归属时间重新计算，保留业务唯一键与已支付状态，避免部署当周额度意外归零后重复发放。迁移前后对账金额及已用额度。
- [x] 同步税收补贴相关配置注释、玩家提示与玩法文档；本项仅修改税收补贴，建筑返还的每周规则不因同样出现“周一”而自动更改。
- [ ] 验收：周一 03:59:59/04:00:00、每天 03:59:59/04:00:00 和 15:59:59/16:00:00 的归属正确；跨周跨年、服务器重启及重试不重复刷新或发放；旧数据升级后配额正确。

## 四、成员通知与领地布局

### 11. 补齐被任免人员的通知（报告 10）

- [x] 从 `TownMembershipActions`、治理相关动作及通知分发链梳理事件矩阵：副镇长任命/免职、其他角色升降、镇长转让/接任、投票导致的任职变更、管理员强制调整；同时核查入镇、移除成员和访客身份变化是否遗漏目标通知。
- [x] 逐项记录操作者、目标玩家、触发入口、成功消息键、在线/离线投递行为及现有去重方式，确认缺失后补齐；避免仅根据动作类未直接发消息就判定整条链路没有通知。
- [x] 通知在状态变更成功提交后发送，明确小镇、原角色和新角色；失败、取消及重复回调不发送成功通知。
- [x] 在线目标即时收到通知；离线目标复用既有持久通知机制，若当前没有则补充最小持久待投递记录及登录投递，保证重启可恢复且不会重复提示。
- [ ] 验收：为各任免路径覆盖在线/离线、成功/失败、重复回调场景；被任免人员收到一次准确通知，操作者反馈不重复。

### 12. 修复领地选择后的列间距变化（报告 16）

- [ ] 对比 `TownTerritoryUi` 初始渲染和选中后重绘所传的页脚动作，复现首次选择、继续选择、取消选择和清空选择。（代码检查与修复已完成；客户端复现待验收。）
- [x] 重点检查 `TerritoryDialogRenderer`：当前地图小按钮与页脚宽按钮加入同一个 `multiAction` 网格，新增页脚可能撑大整列宽度。这是待客户端验证的原因假设，不能只凭单个区域按钮宽度不变就认定布局正常。
- [x] 将地图网格与批量确认/清空等宽操作解耦，优先使用现有 Dialog API 可支持的独立退出动作或独立确认页；若必须共用网格，所有网格项保持一致宽度且操作文案完整可读。
- [ ] 验收：不同 GUI 缩放下，选中前后区域按钮列间距、行间距和地图位置稳定；多选、清空、边界区域与批量确认正常。除布局模型断言外必须做客户端截图对比。

## 五、实施顺序与回归

1. 先处理第 9、10 项时间方案及旧数据兼容，再处理第 1 项代码校验，明确业务规则和存量数据边界。
2. 完成草稿流程、选址状态、多行消息及独立确认键，统一更新消息契约和缺省值兼容。
3. 完成成员通知链核查与修复、领地布局客户端复现与修复。
4. 执行针对性验证，再完成构建和报告逐项验收。

- [x] 扩展现有 `ApplicationText`、申请/规则/领地 Dialog、消息契约及 `EconomyRepositorySqliteTest` 等相关测试；时间边界用固定时钟或显式 `Instant`，不得依赖运行时当前时间。
- [x] 对涉及通知、补贴迁移的变化补充行为验证：失败不通知、成功仅一次、升级前后已用额度与金额一致。
- [x] 使用 JDK 25+、Maven 3.9+ 执行 `mvn verify`；按项目测试规范记录结果，区分自动测试和需客户端人工验证的内容。
- [ ] 在测试服完整走通：创建草稿 → 恢复编辑 → 选址/重选 → 初始成员确认 → 提交 → 审核；再验收任免/接任、补贴边界、领地多选和增益购买确认。
- [x] 配置更新保留现有自定义值，核查旧消息文件缺键回退和多行渲染；只在有引用检查依据时删除废弃键。
- [ ] 最终按 `report.md` 1～17 逐条记录结果、截图或测试证据；全部通过后再勾选本方案对应任务。
