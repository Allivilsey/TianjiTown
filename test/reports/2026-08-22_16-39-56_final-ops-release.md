# 15.10 2026-08-22 16:39:56 运维故障矩阵、最终候选与发布结论

```text
测试轮次：testlist-20260822-final-pass-ops
隔离场景：LocalTestServer/validation/retest20260822-ops-matrix-v4
依赖：Residence 6.0.2.4、Vault 1.7.3-b131、XConomy 2.26.3、WorldBorder 1.19、QuickShop-Hikari 6.3.0.0、Jobs 5.2.6.6、GlobalMarketPlus 1.4.1.4

本轮新增完整通过（16 项）
APP-17、MEM-03、MEM-16、GOV-11～GOV-13、ECO-17、ECO-21～ECO-23、LAND-16、BUFF-13、BONUS-07、TC-10、REL-06、REL-12。

关键运维结果
ECO-21：健康对账前后外部清算余额、内部账户、流水、锁和操作计数完全不变。
ECO-22：外部余额为零时 6 个账户全部锁定；扩张被拒绝，但捐款、查询与对账仍可用；补足后 6 个锁全部解除。
ECO-23：长款不被擅自分配；Vault 查询异常不改变余额、不写流水且不误解锁；离线清算身份和金额精度保持稳定。
ECO-17：热关闭消费后捐款、扩张和 Buff 购买均返回 FEATURE_DISABLED；重新开启后新捐款成功且三方金额一致。
TC-10：生产配置关闭测试命令时，真实 OP 与管理员父权限均不能继承测试接口；直接调用稳定返回 TEST_INTERFACE_DISABLED。

故障注入发现并修复
外部 SQLite 写锁暴露出回滚异常会掩盖原始 SQLITE_BUSY，并污染后续事务。五类 Repository 已统一为显式 BEGIN IMMEDIATE/COMMIT；BEGIN 失败时不执行伪回滚。定向回归及 Storage 34/34 证明原始忙锁原因保留，释放外部锁后新操作恢复成功，旧失败操作不重放。
同时修复了 TestCommand 入镇成功不通知领导、过期镇长转让状态被异常回滚、踢人投票结算目标已缺失仍可能通过，以及生产关闭 TestCommand 时 OP/父权限仍可能看见命令的问题。

失败（2 项）
ECO-03（TT-TEST-20260822-17，P0）：常规退款故障点能保持玩家、清算、内部账户和流水不变；但外部退款本身失败时，操作进入 COMPENSATION_REQUIRED 并锁定消费，玩家余额未自动恢复到原值，不满足清单“全成或恢复原余额”。
REL-13（P0）：因 ECO-03 未通过、P0 环境阻塞及 29 项运维/安全/负载项目尚未执行完整，发布门槛不成立。

环境阻塞（9 项）
ECO-15：当前 QuickShop-Hikari 6.3.0.0 不提供项目要求的动态税事件能力，无法制造真实“税已发生、SQLite 暂不可写”链路；ECO-16 的三类税开关矩阵因此只完成消费开关与热重载部分，继续保持未完成。
OPS-09：缺少 Residence、QuickShop、XConomy 同一时间点的可恢复备份集。
REL-05：本轮不具备连续 24～72 小时窗口。
REL-07～REL-11：缺少目标新旧依赖版本、真实商业事件、生产数据副本或可兼容旧候选包。
REL-14：无生产环境与发布后观察窗口。

最终环境收尾
测试服活动目录只保留候选 TianjiTown.jar；测试桥接插件已移至 backups/testlist-20260822-final-pass/local-test-bridges，可恢复。25565、25575 均关闭。配置、SQLite、Residence 基线 SHA-256 分别恢复为 DCAD09006304093911E93FCE4B76DB6425D1DC3985EC669675BBAD34D5F37455、42A68EF84F5260B04BD2761622DE50469FE851C3D89291290759DF1E51769963、30EA6F7707375D1F752C3ADA990A77AD93B65EEC7DA0671B723F6E108B5B1949；数据库 quick_check=ok、外键违规 0、schema=7.0。测试服与构建候选 JAR 哈希均为 2367B65F32665E6134BC49885BC3122C194C6B7FC97901018DEC245FC7A8302D。

最终状态统计
testlist.md：通过 124 / 失败 2 / 环境阻塞 9 / 未完成 29，共 164 项；AUTO/CMD 已全部完成，剩余项目均为 OPS。
```

#### 未完成项目追踪（29 项）

以下项目在本轮仍为 `[ ]`。每项完成后必须按第 15 节模板补充执行证据，再更新主清单状态。

| 项目 | 未完成原因 | 完成建议 |
|---|---|---|
| CFG-14 | 已经历多轮独立启停，但没有在同一隔离实例内连续执行两次插件 disable/enable，并量化监听器、计划任务和适配器实例数。 | 增加运行时注册计数和任务快照；在固定业务基线下连续 disable/enable 两轮，每轮各触发一次税、结算、Buff、返还和备份，断言事件及副作用都恰好一次。 |
| CFG-15 | 尚未在税重试、投票结算、Dialog、Buff 刷新、诊断和备份分别处于活动窗口时执行正常停服，因此无法证明所有关闭次序。 | 为六类任务增加可控阻塞点，逐场景发送 `stop`；核对拒绝新写、队列刷新/取消、数据源关闭、效果清理、线程退出及重启后幂等恢复。 |
| CFG-16 | 没有在各关键写阶段执行进程级强制终止；现有正常关闭和 Repository 回滚测试不能替代崩溃恢复。 | 在独立数据副本中对建镇、入镇、投票、捐款、税、扩张和 Buff 的每个持久化阶段设置停顿，强制终止 Java 后重启两次，逐项核对资金、状态、投影和幂等键。 |
| DB-14 | 未构造相同毫秒时间戳与多页大数据，现有 Repository 测试只覆盖小规模查询。 | 新增 SQLite 集成测试，批量写入至少 3 页申请、成员、流水和审计，固定相同 `created_at`；按页正反遍历并断言稳定次级键、无重复遗漏和页大小上限。 |
| APP-25 | 申请费、`beginProvision`、Residence 创建和 `finishProvision` 之间尚无可逐点控制的真实故障矩阵。 | 为 Vault、Repository 和 Residence 协调器增加一次性故障点；每点分别测试抛错与进程终止，再重启重试，核对余额、operation、application、投影和审计最终只落定一次。 |
| MEM-08 | 未在 SQLite 成员提交后、Residence 权限同步前注入失败，也未验证自动对账重试。 | 用故障 Residence 适配器令首次授权失败；保留已提交成员记录，恢复依赖后运行对账并重启复核，断言本镇权限补齐、他镇权限未误授且无重复成员。 |
| ECO-13 | 尚不能稳定令服务器补贴的外部清算入账失败并随后恢复，缺少真实重试闭环。 | 使用可切换失败/恢复的 Vault 提供者，在三类税源分别触发补贴失败；核对不写双倍内部账，恢复后按同一 `business_key` 只补一次且外部、内部和流水一致。 |
| ECO-16 | 已验证热重载和消费开关，但当前 QuickShop-Hikari 6.3.0.0 缺少动态税事件能力，三类真实新收入的关闭/开启矩阵未完成。 | 准备具备目标事件 API 的 QuickShop 版本以及可真实成交的 Jobs、GMP 环境；对三类收入各执行关闭前、关闭中、重新开启后三笔交易，并核对旧账可读且新税/补贴只在开启时产生。 |
| ECO-24 | 外部写锁只验证了 Repository 忙锁恢复，尚未在真实插件运行中断开 SQLite 并遍历全部资金入口。 | 使用可恢复的数据库故障代理或受控文件权限令连接和新连接同时失败；执行申请费、捐款、税、补贴、扩张、Buff、退款、查询和对账，恢复后比较全部余额、流水、锁和操作记录。 |
| LAND-11 | 未完整覆盖扣款、占位、Residence area 创建和完成标记四个故障点，尤其缺少真实外部投影失败后的补偿。 | 在扩张协调器四个阶段设置一次性故障；每个场景核对退款、占位释放、`EXPANSION_REFUND`、Residence 区域和重放结果，并额外覆盖退款失败进入人工补偿。 |
| LAND-12 | 尚未分别保留 `PREPARED`、`REFUNDED`、`COMPENSATION_REQUIRED` 状态后执行真实重启。 | 在独立数据库中按合法外键关系构造三类中间状态，部署对应 Residence/余额快照；连续启动两次并运行诊断，断言可恢复项收敛、人工项保持锁定且第二次启动零新增副作用。 |
| BUFF-15 | 只完成 Repository 持久化与部分运行时开关，未对真实 Vault、SQLite 和效果刷新三类故障分别完成恢复闭环。 | 使用可切换故障的 Vault/存储/效果桥接器逐类注入；记录购买前后余额、流水、Buff 记录和在线效果，恢复依赖或重启后断言不重复扣款且未投影效果可补刷。 |
| BONUS-08 | 未在真实在线玩家生命周期中覆盖离线、背包满、世界卸载和插件停用四个发放窗口。 | 用命令行客户端保持玩家在线并填满背包，分别在数据库预留后触发离线、世界卸载和插件停用；核对物品掉落/不发放策略、预留状态、重连结果及零复制。 |
| BONUS-09 | 尚未在功能热关闭期间实际放置目标方块并比较计数和物品变化。 | 固定玩家、世界和方块序列，依次执行开启、关闭、重新开启三个阶段；逐阶段核对抽取结果、周计数、库存/掉落和数据库记录，只允许两个开启阶段产生新结果。 |
| BEACON-08 | 未同时覆盖热关闭、归档、离开世界、插件停用和重启后的托管效果清理。 | 建立带 Buff 信标效果的小镇和在线玩家；逐场景记录药水效果、已加载区块及信标方块快照，执行状态变化和重启，断言托管效果按时移除且方块与历史区块不被扫描或修改。 |
| BEACON-09 | 没有目标规模的多镇、多效果、多在线玩家负载，也未采集刷新任务耗时和区块加载变化。 | 先确定镇数、在线人数和单 tick 预算；生成对应索引与客户端负载，持续采集刷新耗时、TPS、已加载区块和内存，证明处理量只随索引和在线玩家增长。 |
| TC-12 | 已发现并修复单一 SQLite 写锁恢复问题，但未在存储中断期间遍历 TestCommand 的全部写 action。 | 复用 TC-09 的 24 个写 action 矩阵，将数据库故障代理置为不可写/不可用；逐项比较数据库和外部投影前后快照，恢复后只提交新请求并确认旧失败请求不重放。 |
| OPS-01 | 已覆盖 READY、LOCKED、维护、消费锁和 QuickShop 告警，缺少可观测的 CHECKING 窗口及 SQLite 运行时中断状态。 | 在启动门禁加入测试屏障以稳定查询 CHECKING；再通过数据库故障代理制造中断，逐状态保存 `status` 输出并与依赖、能力、维护和消费锁的实际值对照。 |
| OPS-03 | 当前只验证待补偿经济操作和清算锁，未制造数据库异常、失败投影、账本不平、扩张补偿及 Residence 差异的完整集合。 | 为每类异常建立互相独立的数据库/依赖副本，先保存预期异常清单再执行诊断；断言每类定位、总体健康状态、报告证据和零破坏性修复，最后增加混合异常场景。 |
| OPS-04 | 没有可控的 QuickShop 历史数据源来稳定提供不可用、1000 条及超过 1000 条三个边界。 | 使用目标版本 QuickShop 的真实测试库或契约一致的故障适配器，准备 999、1000、1001 条及查询异常场景；核对扫描数量、`INCOMPLETE` 状态和诊断不误报。 |
| OPS-11 | 已验证两个手动诊断互斥，但未让定时入口与手动入口在同一调度窗口竞争。 | 为诊断执行增加一次性屏障，将定时任务固定到已知 tick，并在进入屏障时发送手动命令；验证只有一个实际执行、另一个收到明确提示且目录中只生成一份完整报告。 |
| SEC-04 | 未确定目标生产主机的账户与 ACL 模型，也未系统扫描玩家可见输出和日志中的敏感信息。 | 在目标部署账户下检查配置、SQLite、备份和诊断目录 ACL；以普通玩家遍历命令错误路径，并对日志/报告扫描绝对路径、凭据、堆栈和玩家标识，记录允许项与脱敏规则。 |
| SEC-05 | 尚无能让六个第三方 API 分别抛出 `RuntimeException` 和 `LinkageError` 的运行时故障提供者。 | 为 Residence、Vault、WorldBorder、QuickShop、Jobs、GMP 各建立隔离故障桥接器；在事件和命令入口逐类注入两种异常，监控 Paper 主线程、后续请求和锁定状态。 |
| SEC-06 | 没有在异步回调的精确边界让玩家、世界、区块或插件失效，普通离线/停服测试不足以证明竞态安全。 | 给 Dialog、税重试、信标、预览和诊断回调增加测试屏障；回调暂停时执行断线、卸载或 disable，再释放回调，断言生命周期检查先于对象访问且关闭后无数据源调用。 |
| SEC-07 | 未建立持续的滥用流量、资源预算和判定阈值，零散非法请求不能证明限流和队列有界。 | 定义请求速率、持续时间、TPS/P95/内存/日志上限；用多客户端分别轰击无效命令、重复 Dialog、预览、捐款和投票，采集队列、会话、日志与数据库一致性。 |
| REL-01 | 尚未确定“目标规模”数值和启动性能预算，也没有相应综合数据集。 | 先书面确定小镇、成员、流水、投票、Buff、返还、信标规模及启动时间/内存预算；生成固定种子数据库，冷启动至少三次并保存线程、阶段耗时和内存峰值。 |
| REL-02 | 未准备 1000+ 成员/小镇及长期账本、审计数据，查询预算也未量化。 | 生成超过两级分页边界的固定数据集，定义查询和诊断 P95 上限；分别执行列表、翻页、账本和诊断，检查 SQL 计划、返回上限、主线程耗时和内存。 |
| REL-03 | 缺少能同时驱动 Dialog、投票、资金、交易、扩张、Buff 和方块事件的多玩家负载，以及项目性能预算。 | 确定代表性并发人数和业务比例，用独立客户端与真实商业插件运行阶梯负载；采集 TPS、P95、GC、内存、SQLite 等待、重试队列，并在结束后做资金/领地全量对账。 |
| REL-04 | 尚未让七类周期任务在同一窗口可重复触发，也缺少单任务与组合任务的耗时基线。 | 为各任务提供仅测试服可用的一次性触发和屏障，先测单项再同时释放；记录每 tick 耗时、任务实例数、失败隔离和最终数据，并据结果配置错峰策略后复测。 |

```text
最终结论：NO-GO。候选构建、命令业务和大部分数据边界已收口，但 ECO-03 的资金补偿 P0 失败及多个 P0 恢复/兼容/安全项目仍未关闭，不得进入生产发布。
```

> 本报告从 [自动化、命令与运维测试列表](../testlist.md) 分离，证据路径均相对于项目根目录。
