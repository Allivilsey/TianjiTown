# 2026-09-12 修复实施与验证结果

范围为 [修复方案](2026-09-12-fix-plan.md) 第 1～10、12～14 项。代码、配置文案、管理员/玩家文档及对应测试已完成；第 11 项不在本次范围内。版本保持 `1.0.0-SNAPSHOT`。真实客户端人工验收尚未执行，不将下述服务端测试等同于所有人工用例通过。

## 实施内容

| 报告项 | 已实施行为 | 验证依据 |
| --- | --- | --- |
| 1 | Residence 传送子命令提前放行，支持 res/residence、tp/teleport、命名空间形式；保留写命令保护 | 集成模块命令守卫测试，包括依赖不可用时不查询、不取消传送 |
| 2 | 中文欢迎/离开消息使用小镇展示名称；创建、资料保存、启动同步、自动/手动修复写入受管理领地 | 隔离服务器读取实际 Residence 消息、名称变化快照投影成功 |
| 3 | 任一成员拒绝即令整轮邀请失效；重开进入成员选择；保留资料与有效选址；保存时重发整轮令牌并清除提醒冷却 | SQLite 测试覆盖重启、同人重邀、换人、旧令牌、重复回复、两人同轮并发确认；真实 Dialog 路由已接入 |
| 4 | 维护命令只注册 on/off/status，删除 enable/disable | 命令分发与文档检查 |
| 5 | 按 Residence 实际 padd 配置逐项静默设置，保留 trusted 状态与失败判断 | Residence 6.0.2.4 真实权限/消息测试；编译 API 6.0.0.1 的自动化测试 |
| 6 | 启动诊断成功静默；手动成功一行；失败输出详情与报告路径，完整报告仍保存 | 诊断测试、隔离启动 READY、最终诊断文件 |
| 7 | 删除、成员调整、镇长转移、投票取消、资金/税率调整等真实原因参数可选，默认“管理员调整”；角色仍独立必填 | 解析与命令分发测试，保留多词原因与业务必填校验 |
| 8 | buff set 免费覆盖，默认一周一级，周数 1～4、等级按配置限制；真实补全；独立于商店开关，失败精确恢复旧订单 | SQLite 交易/补偿测试；真实命令在商店关闭时设置二周三级，余额不变；非法周数/等级被拒绝 |
| 9 | 按旧实际支付金额与剩余毫秒向下取整退款；退款与新扣款同事务；确认绑定旧订单/小镇并使用唯一业务键；记录替换关系并精确补偿 | 折扣、退款后资金才足够、并发、重复提交、边界/溢出、恢复后再购买、审计写失败整体回滚测试 |
| 10 | town list 默认 active，显式支持 provisioning/archived，忽略大小写并补全 | 查询/命令测试；隔离 archived 输出三个归档小镇，非法状态明确报错 |
| 12 | 管理命令、帮助、补全统一 tianjitown.admin；移除旧细分节点授权 | 权限与插件描述符、命令分发测试 |
| 13 | 归档事务取消活动 Buff；归档后先停用领地缓存并使旧刷新失效，再刷新在线玩家；与 Residence 删除是否成功解耦；扫描 buff_ 命名空间实际属性 | 事务、生命周期测试；隔离归档后无 Buff 权益、无 ACTIVE Buff；在线实际属性见待验收 |
| 14 | 检查最新申请与关联小镇状态后才查领地/扣款；活动小镇返回既有结果；重试使用小镇领地快照；完成回调一次；提交后通知异常单独记录；入口/会话/操作与堆栈可追踪 | 新建镇运行时测试及三轮真实服务端建镇/归档/重新申请；详细边界见下文 |

实现时保留两项既有约束：资料入口禁止修改镇名/代码，没有新增改名业务；归档后通过刷新所有在线玩家覆盖原成员和待清理玩家，查询其当前权益以免误清新镇效果。离线玩家沿用登录校准。退款唯一键由旧、新订单 ID 共同组成，允许补偿恢复旧订单后的再次合法购买，购买本身仍按独立业务键去重。

## 自动化结果

执行环境：Windows、Java 25.0.4。最终 `mvn -B clean verify` 于 2026-09-12 23:41:58 完成，`BUILD SUCCESS`。

| 模块 | 测试数 | 失败 / 错误 / 跳过 |
| --- | ---: | --- |
| Core | 85 | 0 / 0 / 0 |
| Storage | 76 | 0 / 0 / 0 |
| Integrations | 61 | 0 / 0 / 0 |
| Paper | 338 | 0 / 0 / 0 |
| 合计 | 560 | 0 / 0 / 0 |

记录：仓库根目录 `.codex-verify.log`，各模块 `target/surefire-reports`。`git diff --check` 通过。Residence 发布 POM 的既有依赖元数据警告和 shade 重复资源警告未导致构建失败。

产物：`tianjitown-paper/target/TianjiTown-1.0.0-SNAPSHOT.jar`。

SHA-256：`0E97209FA2B404DA4E231DB07F27DFC9D42C58E01E5737E041DD3C0303829CE1`。

## 隔离服务器实测

独立目录 `LocalTestServer/fix-20260912`（已在本地忽略目录中），仅绑定 `127.0.0.1`，独立平坦世界 `fix-world`，最终结构新库 `plugins/TianjiTown/fix-final.db`。测试后已正常关闭进程。未向原 `testserver` 部署或写入数据库。

实际组合：Leaf 26.2、Java 25、Residence 6.0.2.4、CMILib 1.5.9.9、Vault 1.7.3-b131、XConomy 2.26.3、QuickShop-Hikari 6.3.0.1、Jobs 5.2.6.6、GlobalMarketPlus 1.4.1.4、WorldBorder 1.19。`tianjitown status` 为 READY。

测试辅助插件通过正式仓储、运行时与 Bukkit 命令调用业务；使用三个合成离线身份，没有连接真实 Minecraft 客户端。Residence 测试身份先登记到其玩家管理器，否则从未见过的离线身份没有 trusted 容器。辅助插件源码为 `smoke-src/TownFixSmoke.java`；RCON 密码只在隔离实例配置中，不写入报告。

三轮小镇：

| 展示名称 | 申请 ID | 正常审批时间 | 最终状态 |
| --- | --- | --- | --- |
| amu | d6a09811-f0bb-427e-bbcb-104c32f18ab3 | 23:30:32 | ARCHIVED |
| amu | b4d1f068-49ab-4320-b902-49ecbd84995b | 23:42:35 | ARCHIVED |
| AMU | abc561a8-ec4d-4730-8d7a-f83543d7cd18 | 23:42:38 | ARCHIVED |

每轮先正常审批一次，再单独调用成功后的过期审批路径，后者返回既有成功且 `notifyDecision=false`。三笔建镇费用各 500000 最小货币单位，合计 1500000，无额外建镇入账。后两轮在最终 JAR 上从新申请完成全流程；第一轮还验证了重启后已释放预留的再次审批。中文 enter/leave、padd trusted、免费覆盖余额不变、免费失败补偿恢复准确旧订单、剩余退款、真实免费设置命令及归档权益清理均通过。最终日志含 `SMOKE COMPLETE`。

为避免商店默认价格高于测试小镇余额，付费交易辅助测试使用与 health 效果一致的低价定义；真实 `buff set` 命令使用实际配置。测试过程中有身份未登记、余额不足、资料改名不允许三类夹具错误，修正测试夹具后完成回归；未放宽正式权限/价格/资料校验。较早中断测试留下的订单保留在新库，因此 Buff 总订单数大于三轮正常流程的最小数量。

最终只读检查：三个小镇全部归档；`active_buffs` 为 7 条 CANCELLED、8 条 SUPERSEDED，0 条 ACTIVE；`quick_check=ok`、外键错误 0。最终统一诊断 accountLedgerMismatches、failedProjections、pendingEconomy、pendingExpansions 均为 0，Vault 清算和 QuickShop 对账通过，完整报告保存在隔离实例 `plugins/TianjiTown/diagnostics`。

本地证据：`final-console.log`（首次审批）、`passed-console.log`（最后三轮续测及完成）、`smoke-src/TownFixSmoke.java`、诊断目录；根目录 `.codex-sql-evidence.log`、`.codex-server-commands.log`。RCON 对异步回复的收集不完整，所以不据其空输出声称手动诊断聊天呈现已完成实测。Leaf 在插件初始化前有 Windows OSHI 进程信息空指针警告，调用栈位于服务器系统报告，不是建镇异常。

## 报告 14 的结论与未闭环部分

现场已部署 JAR 的字节码确认原实现已有 Dialog 单次使用和会话消费保护（本地 `.codex-deployed-dialog.log`）。历史日志只证明同一申请成功后再次进入，缺少第二次入口来源及完整堆栈，不能确定是双击、旧回调、延迟或其他触发，也不能声称已复现当时的完整故障。

已修复可确定的空预留解引用顺序错误，并用真实服务端证明正常首次审批、解散后同名/大小写名称重新申请、成功后的再次进入均不会重复扣款或通知决定。自动化还覆盖并发入口 BUSY、失败重试领地来源、已归档拒绝及提交后的缓存/UI 异常。新增来源/会话/操作日志用于原环境若再次出现时继续定位；“最初为什么再次进入”的历史因果仍无法从现有证据补全。

## 人工验收与数据边界

待执行的真实客户端项目已列入 `test/manualtest.md` 的 FIX-2026-09-12 专项：邀请重选 Dialog 全路径、一次真实点击审批及多管理员操作、进出领地和传送、实际建筑操作权限、在线属性/离线重登/HuskSync、删镇投影失败及立即换镇、管理员非 OP 权限和补全、商店确认界面。上述未测项目未勾选为通过。

只修改初始 SQL，新增邀请令牌、Buff 精确替换关系和非负价格约束；无后续迁移、回填、配置版本、兼容层或自动数据库重建。已有开发数据库的 Flyway 校验和不会自动修复，不能将新 JAR 覆盖旧库当作升级流程。原 `testserver` 最后只读检查仍为 6 镇（2 ACTIVE、4 ARCHIVED），完整性正常；保留用户原有数据作为现场证据。
