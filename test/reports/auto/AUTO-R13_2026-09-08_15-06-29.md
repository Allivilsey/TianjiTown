# R13 2026-09-08 15:06:29 异常与边界覆盖审查

## 范围与基线

- 用户指定关注异常与边界场景；审查 `test/testlist.md`、相关业务实现和 Maven 测试，补充可确定复现的缺失变体。
- HEAD：`695839504cf1905446ae820158ad7873cbb003de`。开始时已有 `test/testlist.md` 未提交修改；保留其内容，追加本轮映射。候选源码还包含本轮四个测试类的修改，无生产代码、版本、配置或 SQL 变更。
- JDK 25.0.4、Maven 3.9.12、Windows 11。未启动服务器/MCC，未访问用户游戏数据库。
- 本轮为场景覆盖审查，未采集 JaCoCo 等行/分支覆盖率数据，不能给出代码覆盖百分比。

## 发现与新增测试

| 清单项 | 审查发现 | 本轮新增可执行变体数 |
|---|---|---:|
| APP-02/03 | 已有代码长度、名称规范化和部分非法文本测试，但缺名称/简介/规则的成对精确阈值及简介/规则独立危险文本校验 | 19 |
| CMD-04 | 已有恰好到期确认、所有者隔离和替换请求；缺到期前 1 ms、取消到期/越权/重放、动作异常后令牌消费语义 | 5 |
| ENV-08 | 已有 worker 拒绝、工作失败与退避；缺重试定时器拒绝后恢复，以及恢复后旧定时器回调 | 2 |
| LAND-02/03、ENV-08 | 已有正常拓展、部分无效目标和价格；缺单原点对角/隔格、int 极值、异常已有网格、第 25 格/满格、批量求和溢出 | 13 |
| 合计 | 四个测试类新增 18 个测试方法，参数展开后 39 个执行变体 | 39 |

对应源码：

- `tianjitown-core/src/test/java/org/allivlisey/tianjitown/core/application/ApplicationTextTest.java`
- `tianjitown-core/src/test/java/org/allivlisey/tianjitown/core/land/ExpansionRulesTest.java`
- `tianjitown-paper/src/test/java/org/allivlisey/tianjitown/paper/command/CommandConfirmationManagerTest.java`
- `tianjitown-paper/src/test/java/org/allivlisey/tianjitown/paper/task/RetryingWorkQueueTest.java`

所有新增测试使用固定输入、可推进时钟或独立工作/定时队列，无 sleep、源码字符串匹配或随机概率判定。金额溢出测试分别确认两笔单价为 5,000,000,000,000,000,000 和 5,250,000,000,000,000,000 最小货币单位，再断言总价计算失败。

## 已有覆盖与限制

- `EconomyRulesTest` 已覆盖最小货币单位精度、金额范围、算术溢出、不同精度拒绝和税额舍入（ENV-08/ECO-01/02）。
- `TownRepositorySqliteTest`、`EconomyRepositorySqliteTest`、`GovernanceRepositorySqliteTest` 已有后置审计/账本失败的事务回滚，以及预留、冷却、治理失效和重启后转让过期测试（APP/MEM/GOV/ECO/OPS 相关项）。其中部分到期测试使用当前时间和临时库时间写入，只证明已过期路径，不证明精确等号边界。
- `EconomyRepositorySqliteTest.subsidyBoundariesIgnoreHostZoneAndRetryKeepsOriginalPeriod` 以固定 Instant 检查北京时间 04:00/16:00、周一和跨年刷新，并验证重试使用原周期（ECO-07）。
- 真实交易、玩家 Dialog、进程中断恢复和外部依赖故障仍按清单单独验收。本轮未执行这些 CMD/OPS 项，状态为 NOT_RUN，不能将 513 个 Maven 测试通过换算成整份清单全部通过。

## 执行结果

先执行 `mvn -B clean verify` 成功；补齐领地测试后执行 `mvn -B verify`，最终完成时间 2026-09-08 15:06:21 +08:00，退出码 0，四模块 BUILD SUCCESS。Surefire XML 汇总：

| 模块 | 测试 | 失败 | 错误 | 跳过 |
|---|---:|---:|---:|---:|
| core | 84 | 0 | 0 | 0 |
| storage | 66 | 0 | 0 | 0 |
| integrations | 53 | 0 | 0 | 0 |
| paper | 310 | 0 | 0 | 0 |
| 总计 | 513 | 0 | 0 | 0 |

新增 39 个变体全部通过，未发现需要修改生产实现的失败。测试报告位于各模块 `target/surefire-reports/TEST-*.xml`，最终构建日志为项目根目录 `test-coverage-verify.log`；这些构建产物仅在本机可用，未纳入报告版本管理。已有编译弃用和 shaded JAR 重叠资源警告不影响本轮退出结果。
