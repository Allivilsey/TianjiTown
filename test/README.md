# 测试资料导航

版本库中的测试资料集中存放于此目录：

- [自动化、命令与运维测试列表](testlist.md)：`AUTO`、`CMD` 与 `OPS` 测试项、执行模板和报告索引；
- [P0 人工测试清单](manualtest.md)：按建镇、资产、权限、人事、持久化、故障恢复和解散顺序执行，包含详细步骤、预期结果及报告模板；
- [Dialog UI 改版人工测试清单](UI_DIALOG_MANUAL_TEST.md)：本轮界面改版的角色、表单、分页、确认、会话与显示专项验收；
- [测试报告分类](reports/README.md)：按 AUTO/MANUAL 分类存放；当前已有报告全部归入自动测试；

各 Maven 模块的自动化测试源码继续保留在模块标准的 `src/test` 目录，确保 Maven 能自动发现并执行。`LocalTestServer`、`MinecraftConsoleClient` 与 `backups` 是本机测试环境及证据目录，不纳入版本控制，报告中的路径仍以项目根目录为基准。

## 自动化测试运行与边界

在项目根目录使用 JDK 25 和 Maven 3.9+ 执行 `mvn clean test`。删除或重命名测试后使用 `clean`，避免旧的测试 class 被继续执行。单独验证模块可执行 `mvn -pl tianjitown-storage -am test`。

- 测试断言业务输入、输出和可观察副作用，不读取 Java 源码匹配方法名，也不把文件行数作为通过条件。
- 默认配置测试验证配置可加载、消息可解析、价格可购买；解析和计算测试使用自有固定输入与明确预期，不冻结运营价格。
- 异步单元测试分别排队工作任务和定时任务，由测试推进执行，验证等待、失败、重试和恢复；这不替代真实线程并发测试。
- 数据库测试使用 `@TempDir` 下的独立 SQLite 数据库。并发约束分别验证成员归属、镇长唯一性、副镇长上限、投票互斥和税收入账幂等；线程等待有超时并在结束时释放。
- 单元测试不证明 Paper、Vault 或其他插件在真实服务器中的完整交互正常；这部分仍需服务器集成验收。

## 测试报告命名规范

- 文档标题使用“`序号 YYYY-MM-DD HH:MM:SS 报告名称`”，时间采用项目时区 `Asia/Shanghai`。
- 文件名使用“`<类型>-R<两位轮次>_YYYY-MM-DD_HH-mm-ss.md`”，例如 `AUTO-R01_2026-08-15_02-23-00.md`。文件名只保留报告类型、轮次和时间，不加入报告内容描述；Windows 文件名不能包含冒号，因此文件名中的时、分、秒使用连字符，文档标题和索引仍使用标准的 `HH:MM:SS`。
- 时间标记优先使用该轮报告的完成时间；跨日测试使用结束日期和结束时间。
- 新报告必须记录到秒。迁移旧报告时，优先依据测试编号、证据文件写入时间和轮次顺序估算，并在报告开头注明“推测值”；只有无法建立任何可靠顺序时才使用 `00:00:00`，并注明“未知时刻占位”。
- 自动测试使用 `AUTO-R01`、`AUTO-R02` 递增命名，手动测试使用 `MANUAL-R01`、`MANUAL-R02` 递增命名；同一类型和轮次在同一秒生成多份报告时，在时间后追加两位序号（如 `AUTO-R01_2026-08-15_02-23-00_01.md`）。
