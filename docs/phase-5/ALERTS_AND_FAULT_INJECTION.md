# 告警、故障注入与最终验收

所有故障注入只能在隔离预发服和生产数据副本执行。每个用例前创建同时间点备份，记录开始/结束时间、日志、诊断报告、数据库快照、清算余额和 Residence 状态。

## 故障注入矩阵

| 故障 | 操作 | 必须结果 |
|---|---|---|
| Paper 重启 | 在投票结算、资源领取、Buff 到期、返还计数后重启 | 不重复结算/发物；状态恢复，信标重新接管或清理 |
| SQLite I/O 失败 | 在克隆环境撤销文件写权限或耗尽专用测试卷 | 新写入和返还锁定，Residence 保护保持；恢复后可诊断/继续 |
| Residence 丢失 | 移除测试镇投影后执行自动/手动对账 | 不产生无保护 ACTIVE 镇；安全归档或显式修复有审计 |
| Vault 失败 | 暂停 Economy provider 或使清算调用失败 | 消费锁定，补偿状态可定位，不产生负余额或静默丢款 |
| QuickShop 回滚 | 触发失败/取消交易并重放成功事件 | 失败不入账，成功幂等；历史差异在诊断中可见 |
| 依赖不可用 | 分别移除/禁用必需依赖启动 | 启动门禁 `LOCKED`，现有数据不被重写 |
| 信标失效 | 拆金字塔、拆信标、卸载区块、归档领地 | 原范围和附加效果完整清理，不改第三方自定义信标 |
| 返还竞态 | 概率 100%、并发快速放置并跨重启 | SQLite 原子日上限不超发；特殊/自动放置为零 |

## 最终验收记录

- [ ] 玩家没有小镇命令，只通过服务台/手册完成所有玩家流程。
- [ ] 管理员能审批、代办、对账、修复、诊断和备份每类操作。
- [ ] 并发申请/扩张/消费/投票/领取/返还均保持唯一与幂等。
- [ ] SQLite、Residence、Vault 清算账户和 QuickShop 历史统一报告可定位每个差异。
- [ ] 所有高风险操作可审计、可重试且不会重复扣款或入账。
- [ ] 重启和六类故障注入全部通过，没有 ACTIVE 无保护小镇。
- [ ] schema `4 → 5` 增量升级、在线备份、隔离恢复及整套 JAR/数据回滚通过。
- [ ] 大成员列表分页、180 天账本、规则/税率告知和基础资料 SQLite 单一入口通过。

完成后将报告、日志、版本、配置脱敏副本、备份清单和 SHA-256 保存到发布档案；只有全部项目有证据时才能勾选 `TODO.md` 的 `1.4.0` 生产门槛。
